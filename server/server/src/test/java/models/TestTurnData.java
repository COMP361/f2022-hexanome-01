package models;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import ca.mcgill.splendorserver.models.TurnData;

public class TestTurnData {
  @Test
  public void test() {
//	  TurnData test = new TurnData(-1, -1, -1);
//	  assertEquals(test.getColCardTaken(), -1);
//	  assertEquals(test.getRowCardTaken(), -1);
//	  assertEquals(test.getNobleTaken(), -1);
  }
}
